import React from 'react';
import Filter from "./Filter/Filter";

const Streams = () => {
    return (
        <main className="streams">
           <Filter/>
            <section>

            </section>
        </main>
    );
};

export default Streams;